language: python
python:
  - "3.6"

# command to install dependencies
install:
  - pip install -r kdrems/requirements.txt
  - pip install coveralls


services:
  - mysql

before_script:
  - mysql -e "CREATE DATABASE testdb;" -U root

env:
  - DB_NAME="testdb" DB_USER="root" DB_PASS="" DB_HOST="127.0.0.1" DB_PORT="3306"
  
  
# command to run tests
script:
  - pytest --cov=app

  
after_success:
  - coveralls